# 滚动轴承预测性维护监测系统课程设计报告
姓名：康浩然 学号：202412020105

## 摘要
滚动轴承是旋转机械设备的核心零部件，轴承故障极易引发设备停机，造成生产经济损失。本课程设计基于CWRU滚动轴承振动数据集，完成数据转换、清洗、时频域特征提取，构建故障分类、自编码器异常检测、LSTM 剩余寿命预测三大模型；基于FastAPI实现后端接口服务，使用SQLite数据库完成数据持久化；前端采用HTML+JavaScript+ECharts开发B/S可视化监测页面，实现振动波形预览、文件上传、智能诊断、历史记录查询完整业务流程。系统完成从原始振动数据输入到故障识别、异常告警、剩余寿命预测、维护建议输出的整套智能预测性维护闭环。

**关键词**：滚动轴承；振动信号；故障分类；异常检测；剩余寿命预测；FastAPI；B/S 架构

## 目录
1. [绪论](#1-绪论)
    - 1.1 [项目研究背景](#11-项目研究背景)
    - 1.2 [项目研究目标](#12-项目研究目标)
    - 1.3 [技术栈](#13-技术栈)
2. [数据集与数据预处理](#2-数据集与数据预处理)
    - 2.1 [数据集来源](#21-数据集来源)
    - 2.2 [预处理的流程](#22-预处理的流程)
    - 2.3 [预处理的结果](#23-预处理的结果)
3. [算法模型设计](#3-算法模型设计)
    - 3.1 [故障分类模型](#31-故障分类模型)
    - 3.2 [自编码器异常检测模型](#32-自编码器异常检测模型)
    - 3.3 [LSTM 剩余寿命预测模型](#33-lstm-剩余寿命预测模型)
    - 3.4 [模型推理逻辑](#34-模型推理逻辑)
    - 3.5 [算法测试的结果](#35-算法测试的结果)
4. [后端服务设计](#4-后端服务设计)
    - 4.1 [数据库表设计](#41-数据库表设计)
5. [前端可视化系统设计](#5-前端可视化系统设计)
    - 5.1 [主要功能模块](#51-主要功能模块)
    - 5.2 [前端开发典型问题](#52-前端开发典型问题)
6. [系统部署与运行流程](#6-系统部署与运行流程)
    - 6.1 [环境准备](#61-环境准备)
    - 6.2 [系统启动步骤](#62-系统启动步骤)
    - 6.3 [业务测试流程](#63-业务测试流程)
7. [结果分析](#7-结果分析)
    - 7.1 [模型训练结果](#71-模型训练结果)
    - 7.2 [系统业务效果](#72-系统业务效果)
8. [总结与展望](#8-总结与展望)
    - 8.1 [项目完成总结](#81-项目完成总结)
    - 8.2 [不足与后续改进方向](#82-不足与后续改进方向)
- [参考文献](#参考文献)

## 1 绪论
### 1.1 项目研究背景
现代工业旋转机械设备大量使用滚动轴承，轴承长期运行会出现磨损、点蚀等损伤，演化成内圈故障、外圈故障、滚动体故障，严重时造成整机失效。传统定期维护存在维护过剩或者维护不足的问题，预测性维护依靠振动传感器采集信号，通过机器学习算法实现故障识别、异常判断、剩余使用寿命预测，在故障发生前给出维护建议，降低停机风险。

### 1.2 项目研究目标
1.完成CWRU数据集预处理：mat 原始文件转为 csv，完成信号清洗、滤波降噪、滑窗切片、时域频域特征提取，划分训练集、测试集，生成标准化数据集。
2.搭建三组深度学习模型：故障4分类模型、自编码器异常检测模型、LSTM‑RUL剩余寿命预测模型，完成模型训练与权重保存。
3.开发FastAPI后端服务，实现文件上传、算法诊断推理、数据库存储、历史记录查询接口。
4.开发Web前端可视化页面，实现振动波形绘图、人机交互，调用后端接口展示诊断结果。
5.完成整套系统部署调试，形成完整B/S架构轴承预测性维护监测平台。

### 1.3 技术栈
- 算法层：Python、PyTorch、Numpy、Pandas、Scipy、Scikit‑learn
- 后端服务：FastAPI、SQLAlchemy ORM、SQLite轻量数据库
- 前端：HTML、JavaScript、ECharts5可视化图表库
- 数据处理：mat文件解析、FFT频域变换、滑动窗口采样、StandardScaler 标准化

## 2 数据集与数据预处理
### 2.1 数据集来源
本项目采用CWRU凯斯西储大学滚动轴承公开数据集，原始格式为`.mat`文件，本项目选取4种工况，共8组样本：正常工况、内圈故障、外圈故障、滚动体故障，做4分类任务。

表2‑1 数据集标签映射表

| 文件名 | 标签 | 工况说明 |
| ---- | ---- | ---- |
| normal.csv | 0 | 正常状态 |
| 98normal.csv | 0 | 正常状态 |
| inner.csv | 1 | 内圈故障 |
| 106inner.csv | 1 | 内圈故障 |
| outer.csv | 2 | 外圈故障 |
| 131outer.csv | 2 | 外圈故障 |
| ball.csv | 3 | 滚动体故障 |
| 119ball.csv | 3 | 滚动体故障 |

### 2.2 预处理的流程
预处理由`all_data_tool.py`通用工具库与`data_preprocess.py`主程序共同完成，完整流程如下：
1. **mat转csv**：读取mat文件提取DE_time振动时序信号，输出单列字段为`vibration`的csv文件
2. **滤波降噪**：采用滑动平均滤波，消除原始信号中的高频噪声干扰
3. **滑动窗口切片**：设置窗口大小256，步长128，将长时序振动信号切分为多个信号片段。
4. **特征工程**：提取6维时域特征（均值、方差、均方根、峰值、峭度、偏度）；对信号片段做FFT变换，提取3维频域特征（FFT幅值均值、方差、最大值），组合得到9维时频联合特征向量作为模型输入。
5. **数据集划分**：采用分层抽样按照8:2划分训练集、测试集，保证各类别样本数量比例均衡。
6. **标准化处理**：基于训练集拟合StandardScaler并保存`scaler.pkl`，推理阶段加载同一标准化器，避免训练与推理数据处理逻辑不一致。
7. **输出数据集文件**：X_train.npy、y_train.npy、X_test.npy、y_test.npy。

 ### 2.3 预处理执行顺序：
python all_data_tool.py    # mat批量转换csv

python data_preprocess.py  # 生成预处理数据集


## 3 算法模型设计
本系统一共设计 3 个模型，分别完成故障分类、异常检测、剩余寿命预测。模型训练入口为`test_algorithm.py`，训练完成后权重自动保存`data/model_weight`目录。

### 3.1 故障分类模型
故障分类模型文件为`fault_classify.py`，输入为 9 维时频联合特征向量，输出 4 种故障类别以及对应置信度。网络采用全连接神经网络，完成 4 分类任务，损失函数选用交叉熵损失。训练完成后测试集分类准确率达到 93.74%，通过混淆矩阵可以观察各类样本识别效果。

### 3.2 自编码器异常检测模型
自编码器模型文件为`ae_anomaly.py`，属于无监督学习模型，仅使用正常轴承样本参与训练。模型学习正常样本特征编码‑重构过程；当输入故障样本，重构误差会显著增大。系统设置判定阈值，当重构误差超过阈值，则判定设备处于异常状态。

### 3.3 LSTM 剩余寿命预测模型
LSTM 模型文件为`lstm_rul.py`，LSTM 网络擅长处理时序序列数据。输入连续多段时频特征构成的时序片段，输出轴承剩余使用寿命 RUL 数值。
根据 RUL 数值生成维护建议规则：
- RUL＜20：警告，尽快停机检修；
- 20≤RUL＜60：设备逐步老化，密切监测；
- RUL≥60：设备状态良好，定期巡检。

### 3.4 模型推理逻辑
后端接收网页上传文件的 `raw_data_id`，读取原始振动信号，复用和训练阶段完全同一套预处理工具函数，完成滤波、滑窗、特征提取、标准化，加载训练完成的模型权重，依次调用三个模型，得到故障类型、异常标记、RUL 预测结果，全部写入数据库保存。

## 4 后端服务设计
后端使用 FastAPI 框架开发，搭配 SQLAlchemy ORM 操作 SQLite 本地数据库。

| 文件 | 说明 |
| ---- | ---- |
| Main_api.py | API 主服务，接口、跨域处理、业务逻辑 |
| db.py | 数据库连接与会话管理 |
| models.py | 数据表 ORM 模型定义 |

### 4.1 数据库表设计
系统共设计 4 张数据表：
1. **RawVibrationData**：保存上传文件信息：文件名、采样点数、文件保存路径、上传时间。
2. **FaultDiagnosisRecord**：故障诊断记录，关联原始数据 ID，存储故障类型、分类置信度。
3. **AnomalyAlertRecord**：异常检测记录表，存储是否异常标记、重构误差。
4. **RulPredictRecord**：寿命预测记录表，存储 RUL 预测值、维护建议文本。

## 5 前端可视化系统设计
前端为单页面`index.html`，技术栈为 HTML+JavaScript+ECharts5。不可直接双击打开本地 html 文件，file 协议会触发浏览器跨域安全限制；需要通过 `python -m http.server 8080`启动静态网页服务，浏览器访问`http://127.0.0.1:8080`。

### 5.1 主要功能模块
- **文件选择与波形预览**：浏览器本地解析 csv 文件；针对大数据自动降采样，防止页面卡顿；ECharts 图表支持鼠标框选缩放、视图还原、图表另存为图片，提供清空波形按钮。
- **文件上传**：调用后端 upload 接口，上传成功自动回填 raw_data_id 输入框；按钮置灰防止重复提交。
- **智能诊断模块**：点击执行诊断显示 loading 动画；诊断结果卡片视觉区分：设备正常绿色左侧边框，异常红色告警边框；格式化展示故障类型、置信度、重构误差、RUL、维护建议。
- **历史记录表格**：表格隔行变色，设置滚动容器，大量记录不会撑爆页面。
- **状态栏**：实时显示系统运行状态：就绪、解析中、上传中、诊断完成、异常报错。

### 5.2 前端开发典型问题
1. ECharts 旧配置`xAxis.index`新版本不再支持，修改为`type:"value"`解决图表渲染报错。
2. file 协议打开网页触发浏览器安全跨域警告，改用 http 静态服务。
3. 设置按钮 disabled 状态，避免用户短时间重复点击，重复发起请求。

## 6 系统部署与运行流程
### 6.1 环境准备
使用 conda 虚拟环境`cv_tutorial`；算法、后端运行需要虚拟环境，前端静态 http 服务不需要虚拟环境。

### 6.2 系统启动步骤
python all_data_tool.py

python data_preprocess.py

python test_algorithm.py

### 窗口1，启动后端服务
python main_api.py

### 后端监听地址127.0.0.1:8000

### 窗口2，启动前端静态服务

python -m http.server 8080
###  浏览器访问：http://127.0.0.1:8080

### 6.3 业务测试流程
1. 网页选择csv振动文件，点击预览波形，页面绘制时域振动曲线。
2. 点击上传文件，后端保存文件并返回raw_data_id。
3. 点击执行诊断，页面展示整套算法输出结果。
4. 点击刷新历史记录，查看数据库全部历史诊断记录。

## 7 结果分析
### 7.1 模型训练结果
故障分类模型测试集准确率93.74%，混淆矩阵显示大部分样本可以正确识别；少量样本存在类别混淆，数据集本身存在信号重叠，属于正常现象。
自编码器可以有效区分正常与故障样本；
LSTM可以输出合理的剩余寿命预测数值，配套维护建议可以给运维提供参考。

### 7.2 系统业务效果
系统实现B/S架构，用户仅需要浏览器即可完成整套轴承振动数据诊断。
1. 振动波形可视化支持缩放查看局部细节，大数据自动降采样保证页面流畅。
2. 诊断结果视觉区分正常/异常状态，直观展示故障信息、寿命预测与维护建议。
3. 全部诊断记录持久化存入SQLite 数据库，可以随时查询历史诊断记录，实现可追溯。

## 8 总结与展望
### 8.1 项目完成总结
本课程设计完成滚动轴承预测性维护监测系统开发。
完成CWRU数据集mat转csv、信号清洗滤波、滑窗切片、时频域特征提取整套预处理流程，输出标准化训练测试数据集。
搭建故障分类、自编码器异常检测、LSTM‑RUL剩余寿命预测三套深度学习模型，完成训练与权重保存。
使用FastAPI开发后端接口，结合SQLAlchemy+SQLite完成文件、诊断结果持久化存储。
开发ECharts可视化Web前端，实现波形预览、上传、诊断、历史查询等完整业务闭环。
完成系统部署调试，解决开发过程中出现的路径导入、跨域、图表渲染等各类bug，系统可以完整运行。

### 8.2 不足与后续改进方向
模型泛化能力受限，当前仅使用CWRU数据集，后续可以增加更多不同工况、不同转速的真实工业数据集，提升模型通用性。
当前系统处理离线csv文件，不支持实时流式振动信号接入；后续可以引入消息队列实现实时数据采集与在线诊断。
异常判定阈值为固定值，后续可以引入自适应阈值算法，根据设备工况动态调整告警阈值。
可以增加模型增量学习功能，线上使用新数据对模型持续迭代优化。

## 参考文献
[1] CWRU.Case Western Reserve University Bearing Data Center Dataset .
[2] 杨开忠。基于深度学习的旋转机械故障诊断研究.
[3] FastAPI官方文档.
[4] ECharts可视化开发文档.




