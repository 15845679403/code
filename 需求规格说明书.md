# 滚动轴承预测性维护监测系统 需求规格说明书

## 文档信息
|项目|内容|
|----|----|
|项目名称|滚动轴承预测性维护监测系统|
|文档名称|需求规格说明书|
|文档版本|V1.0|
|编写日期|2026‑09‑02|
|开发环境|Windows 、Python3.10、Conda虚拟环境 cv_tutorial|
|架构模式|B/S浏览器/服务器架构|

# 1 引言
## 1.1 编写目的
本需求规格说明书用于明确**滚动轴承预测性维护监测系统**全部业务需求、功能需求、非功能需求、数据及接口需求。作为系统设计、编码开发、系统测试的依据，同时作为课程设计交付文档。本文档面向开发人员、测试人员、课程设计评审教师。

## 1.2 项目背景
滚动轴承是旋转机械核心零部件，轴承故障会引发设备停机，造成生产损失。传统定期维护存在维护过剩或者维护不足的弊端。
本系统基于CWRU轴承振动数据集，利用深度学习实现故障分类、异常检测、剩余寿命预测，搭建B/S架构Web监测平台，实现离线振动数据智能诊断，为设备运维提供辅助决策。

## 1.3 项目范围
本系统实现离线振动数据处理与智能诊断。
包含：mat数据集转换、信号预处理、特征工程；故障分类、自编码器异常检测、LSTM剩余寿命预测模型；FastAPI后端服务；ECharts可视化Web前端；SQLite本地数据持久化。
本系统**暂不支持工业实时流式数据接入、远程部署、多用户权限管理**。

## 1.4 参考资料
[1] CWRU凯斯西储大学轴承数据集公开文档

[2] FastAPI官方开发文档

[3] ECharts5 可视化开发文档

# 2 总体描述
## 2.1 产品概述
系统为B/S架构预测性维护监测平台。用户在浏览器上传轴承振动CSV文件，系统完成信号解析、算法推理，输出故障类型、异常状态、剩余使用寿命与维护建议；支持波形可视化与历史诊断记录查询。
系统整体分为四层：数据层、算法层、后端服务层、Web展示层。

## 2.2 用户特征
- 本系统面向课程设计使用用户，用户具备基础计算机操作能力；
- 用户无需掌握Python、深度学习算法知识，仅通过浏览器即可完成操作。

## 2.3 运行环境
### 服务器端（本机）
- 操作系统：Windows10 / Windows11
- Python环境：conda虚拟环境 `cv_tutorial` Python3.10
- 依赖库：PyTorch、Numpy、Pandas、Scipy、Scikit‑learn、FastAPI、uvicorn、SQLAlchemy
- 数据库：SQLite（文件型本地数据库，无需额外安装服务）

### 客户端
- 设备：普通PC电脑
- 浏览器：Chrome、Edge主流现代浏览器

## 2.4 设计约束
1. 全部代码模块化，算法、后端、前端代码目录分离；
2. 模型训练阶段与推理阶段必须复用同一套信号预处理代码；
3. 前端页面不能使用本地file协议打开，必须使用http静态服务；
4. 异常告警阈值采用固定阈值；
5. 仅支持离线`.csv`振动文件输入；
6. 项目数据、权重文件不提交至版本库（可配置gitignore忽略）。

# 3 具体需求
## 3.1 功能性需求
### 3.1.1 数据处理模块需求
1. 能够读取原始`.mat`数据集文件，提取DE/FE振动时序信号，批量转换输出单列`vibration`的CSV文件。
2. 振动信号预处理：滑动平均滤波完成降噪；设置窗口256、步长128完成滑动窗口切片。
3. 特征提取：提取6维时域特征（均值、方差、均方根、峰值、峭度、偏度）；FFT变换提取3维频域特征，拼接为9维时频联合特征向量。
4. 数据集按照8:2分层划分训练集、测试集，保证各类样本比例均衡。
5. 使用训练集拟合`StandardScaler`标准化器，保存`scaler.pkl`；推理阶段加载该文件做标准化。
6. 输出npy格式数据集文件，便于模型读取训练。

### 3.1.2 算法模型模块需求
1. **故障分类模型**：输入9维时频特征向量，完成4分类任务：正常、内圈故障、外圈故障、滚动体故障；输出故障类别以及预测置信度。
2. **自编码器异常检测模型**：无监督模型，基于正常样本训练；计算输入样本重构误差，超过阈值判定设备异常。
3. **LSTM剩余寿命预测模型**：接收时序特征序列，输出RUL剩余寿命数值；根据RUL自动生成维护建议。
    - RUL＜20：警告，尽快停机检修
    - 20 ≤ RUL ＜60：设备逐步老化，密切监测
    - RUL ≥60：设备状态尚可，定期巡检
4. 训练完成自动保存模型权重pth文件；推理加载权重完成预测。

### 3.1.3 后端服务模块需求
1. 文件上传接口：接收前端上传CSV振动文件，保存本地磁盘；将文件元信息写入SQLite数据库，返回`raw_data_id`。
2. 诊断执行接口：接收`raw_data_id`，读取对应振动文件，调用全套算法；故障、异常、RUL预测结果写入数据库，返回诊断JSON结果。
3. 历史记录查询接口：多表联查四张数据表，返回全部历史诊断记录，供前端表格渲染。
4. 开启CORS跨域中间件，允许前端跨域访问API。
5. 处理Python路径导入问题，保证后端可以正常导入algorithm算法模块。
6. SQLAlchemy ORM维护四张数据表：
    - RawVibrationData：原始振动文件信息
    - FaultDiagnosisRecord：故障诊断记录
    - AnomalyAlertRecord：异常告警记录
    - RulPredictRecord：剩余寿命预测记录

### 3.1.4 Web前端模块需求
1. 本地解析CSV振动数据，ECharts绘制时域振动波形；支持框选缩放、视图重置、图表保存；大数据自动降采样避免页面卡顿。
2. 文件选择上传功能；上传成功回填返回的`raw_data_id`。
3. 执行诊断时展示loading加载动画，提交按钮置灰，防止重复点击重复请求。
4. 诊断结果卡片视觉区分状态：正常使用绿色边框，异常使用红色告警边框；展示故障类型、置信度、重构误差、RUL、维护建议。
5. 历史诊断记录滚动表格展示，表格隔行变色。
6. 状态栏实时反馈系统状态：就绪、解析中、上传中、诊断完成、异常报错。
7. 提供清空波形按钮。

## 3.2 非功能性需求
### 3.2.1 可靠性需求
1. 诊断结果、上传文件信息持久化存储至SQLite数据库，程序重启数据不丢失。
2. 训练与推理预处理逻辑完全一致，避免数据处理不一致带来预测偏差。
3. 接口异常有返回提示，不会直接崩溃退出。

### 3.2.2 易用性需求
1. 图形化Web操作界面，用户仅使用浏览器，不需要运行代码。
2. 页面状态提示清晰，错误信息直观展示。

### 3.2.3 可维护性需求
1. 项目目录清晰，模块解耦，算法、后端、前端分离。
2. 关键代码注释完整，配套数据集说明文档。
3. 接口职责单一，便于修改扩展。

### 3.2.4 性能需求
1. 数万点振动信号前端降采样绘图流畅无明显卡顿。
2. 单份振动文件算法诊断在普通PC下可快速完成。

### 3.2.5 兼容性需求
前端兼容Chrome、Edge现代浏览器；后端在Windows系统正常运行。

## 3.3 数据需求
### 3.3.1 输入数据
- 原始数据：CWRU `.mat`轴承振动数据集；
- 用户输入：单列`vibration`字段的CSV振动文件。

### 3.3.2 输出数据
1. 预处理输出：`X_train.npy`、`y_train.npy`、`X_test.npy`、`y_test.npy`、`scaler.pkl`
2. 模型输出：故障类别、置信度、重构误差、RUL数值、维护建议文本。

### 3.3.3 数据库数据
数据库保存字段：文件名、采样点数、文件保存路径、上传时间、故障类型、置信度、是否异常、重构误差、RUL预测值、维护建议。

## 3.4 外部接口需求
### 3.4.1 API接口列表
|接口地址|请求方式|接口功能|
|---|---|---|
|`/api/upload`|POST|接收CSV文件，保存文件、写入原始数据表，返回raw_data_id|
|`/api/run_diagnose`|POST|传入raw_data_id，执行整套算法，诊断结果入库，返回诊断JSON|
|`/api/get_history`|GET|联表查询全部历史诊断记录，返回前端渲染表格|

# 4 其他需求
## 4.1 故障处理需求
1. 文件不存在、ID不存在返回业务错误提示，不抛出服务崩溃；
2. 前端网络请求失败，页面给出提示信息。

## 4.2 可扩展性需求
1. 后续可扩展支持更多类型故障工况数据集；
2. 预留接口扩展点，未来可接入实时流式振动数据；
3. 可增加自适应异常阈值、模型增量学习功能。
